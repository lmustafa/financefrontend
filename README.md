# financefrontend

How to Run:

npm install

pip install flask
pip install flask_cors
pip install scikit-learn
pip install numpy
pip install matplotlib
pip install pandas

node server.js
cd financefrontend/backend
python app.py
